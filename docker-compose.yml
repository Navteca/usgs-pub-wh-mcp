services:
  usgs-mcp:
    build: .
    container_name: usgs-publications-mcp
    ports:
      - "8000:8000"
    environment:
      # Authentication (must be provided via .env or environment)
      - USGS_MCP_API_KEY=${USGS_MCP_API_KEY}
      - USGS_MCP_BEARER_TOKEN=${USGS_MCP_BEARER_TOKEN}
      # Transport: streamable-http exposes POST /mcp (required for .../mcp URLs and MCP Inspector)
      - MCP_TRANSPORT=streamable-http
      - MCP_HOST=0.0.0.0
      - MCP_PORT=8000
      # Stateless HTTP mode: avoid session lookup/session-not-found 404 behavior
      - FASTMCP_STATELESS_HTTP=true
      # Allow ngrok host so remote clients can connect (comma-separated if you add more)
      - MCP_ALLOWED_HOSTS=ce6c-93-156-195-113.ngrok-free.app
      # Security configuration (override defaults as needed)
      - USGS_MCP_RATE_LIMIT_REQUESTS_PER_MINUTE=60
      - USGS_MCP_RATE_LIMIT_REQUESTS_PER_HOUR=1000
      - USGS_MCP_MAX_PAGE_SIZE=100
      - USGS_MCP_AUDIT_LOGGING_ENABLED=true
    restart: unless-stopped
    # Resource limits
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 256M
        reservations:
          cpus: '0.1'
          memory: 128M
    # Health check
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    # Security options
    security_opt:
      - no-new-privileges:true
    read_only: true
    tmpfs:
      - /tmp
