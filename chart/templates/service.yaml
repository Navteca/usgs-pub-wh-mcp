apiVersion: v1
kind: Service
metadata:
  name: {{ include "usgs-pub-wh-mcp.fullname" . }}
  labels:
    {{- include "usgs-pub-wh-mcp.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "usgs-pub-wh-mcp.selectorLabels" . | nindent 4 }}
