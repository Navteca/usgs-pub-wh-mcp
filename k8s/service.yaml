apiVersion: v1
kind: Service
metadata:
  name: usgs-publications-mcp
  namespace: mcp-servers
  labels:
    app: usgs-publications-mcp
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP
  selector:
    app: usgs-publications-mcp
